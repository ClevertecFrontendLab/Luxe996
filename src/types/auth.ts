export type LoginFormType = {
    email: string;
    password: string;
    remember: boolean;
};

export type ResultProps = {
    icon: React.ReactNode;
    title: string;
    text: string;
    textBtn: string;
    pathBtn: string;
    testData: string;
};
